import React from "react";
import { usePageTitle } from "../../../hooks/usePageTitle";

function Orders() {
  usePageTitle("لیست سفارشات | فروشگاه اینترنتی دیجیکالا");
  return <h1>صفحه‌ی مورد نظر یافت نشد!</h1>;
}

export default Orders;
